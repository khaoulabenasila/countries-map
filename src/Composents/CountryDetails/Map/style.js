import { makeStyles } from "@mui/styles";
const useStyle = makeStyles(() => ({
  popupText: {
    fontSize: "1.2rem",
    color: "#060047",
    fontWeight: "bold",
  },
  mapContainer: {
    height: "100vh",
    margin: "auto",
    width: "85%",
    marginBottom: "20px",
  },
}));

export default useStyle;
